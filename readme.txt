OM Logistics Business App

This first combined build preserves the three supplied working HTML modules without rewriting their internal business logic. index.html provides one app shell and navigation while each original module runs unchanged inside its own view.

Deploy the entire folder to Netlify as a static site; index.html is the entry point.
